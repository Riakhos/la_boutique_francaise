{% extends 'base.html.twig' %}

{% block body %}
    <div class="container my-5 w-auto">
        <h1>Espace membre privée! ✅</h1>
        <a href="{{ path('app_logout') }}">Me déconnecter</a>
        <a href="{{ path('app_logout') }}">Modifier mon mot de passe</a>
    </div>
{% endblock %}
